# Android TV → webOS/Tizen Tam Parity Planı

Kaynak referans: Android TV native (`android/app/src/main/java/com/streamlivex/android/tv/`, ~19.700 satır).
Hedef: `smart-tv/tv-ui/` (webOS+Tizen ortak React kod tabanı).
Kural: Android TV = kesin referans. Native'de olmayan hiçbir ekran/özellik/davranış eklenmez. Native'de olan hiçbir şey eksik bırakılmaz. Platform farkı YALNIZCA OS'un zorunlu kıldığı player/remote/lifecycle/manifest/packaging adaptörlerinde olabilir.

Tam envanter kaynağı (bu oturumda üretildi, silinmemeli):
`C:\Users\SEDAT\AppData\Local\Temp\claude\C--Users-SEDAT-Desktop-streamlixe\4fe110e3-fb06-40e7-afa7-d89bd81e06c4\scratchpad\inventory-*.md` (5 dosya)

## Faz Durumu

- [x] **Faz 0 — Foundation**: tamamlandı.
  - `scope.ts` (yeni): `TvActiveScope`+`TvProfilePolicy` eşdeğeri, `storageKey()` native sanitize mantığıyla birebir.
  - `playlists.ts` (yeni): çoklu playlist registry (`sameProvider` tekilleştirme, legacy `slx-tv-provider` migrasyonu).
  - `profiles.ts` (yeni): PIN (Web Crypto SHA-256 hex, native `MessageDigest` ile aynı format), `kotlinHashCode()` (native `String.hashCode()` birebir — avatar paleti seçimi için).
  - `PlaylistManager.tsx`, `ProfileManager.tsx` (yeni ekranlar) — native `TvPlaylistManagerScreen`/`TvProfileManagerScreen`+`TvEditProfileScreen`+`TvCreateProfileScreen` birebir (buton metinleri, 6 sabit emoji, PIN akışları, validasyon mesajları).
  - `ProfileSelect.tsx`: 6-profil sınırı kaldırıldı, PIN modalı eklendi, kilit ikonu+"Çocuk" etiketi eklendi, "Profilleri Yönet"/"+" gerçek `ProfileManager`'a bağlandı.
  - `Setup.tsx`: "Demo" modu ve `Provider.demo` alanı kaldırıldı (native'de yok); `onBack` desteği eklendi (Playlist Manager'dan "yeni liste ekle" akışı için).
  - `favorites.ts`: `slx-tv-fav` → scope-bazlı `slx-tv-fav-${storageKey()}`.
  - `i18n.ts`: native `TvStrings`'in eksik anahtarları (director/cast/similar_movies/seasons_episodes/vb.) 6 dilde eklendi.
  - `TvApp.tsx`, `Settings.tsx`: playlist registry entegrasyonu, `settingsRoute` routing ("playlists"|"profiles"), scope aktivasyonu.
  - Doğrulama: `npx vite build --config vite.tv.config.ts` ve `npx tsc --noEmit -p tsconfig.json` temiz (yalnızca kapsam dışı, önceden var olan `app/PlayerApp.tsx` hatası kaldı — dokunulmadı).
  - **Bilinmeyen/doğrulanmamış nokta**: Xtream bağlantı doğrulaması (native `XtreamClient().loadLiveLibrary` başarı kontrolü) web `XtreamForm`'da henüz yok — form doğrudan `onComplete` çağırıyor, sunucuya gerçek bağlantı testi yapmıyor. Bu, native davranışından bir sapma ama önceki fazlarda da vardı (Faz 0 kapsamı dışı, mevcut halinde bırakıldı — Faz 1/6'da ele alınmalı).
- [x] **Faz 1 — İçerik**: tamamlandı.
  - `history.ts` (yeni): izleme ilerlemesi/geçmişi (native `TvContentStore` eşdeğeri) — `saveProgress`/`progressFor`/`isWatched`/`toggleWatched`/`restart`/`removeFromHistory`/`clearViewingHistory`/`continueWatching`/`latestUnfinishedEpisode`, scope-bazlı.
  - `data.ts`: `fetchTrending`, `matchLocalToTmdb` (kullanılmadı, ileride gerekebilir), `fetchTmdbDetail` (backdrop/yönetmen/oyuncu/öneri/fragman), `fetchPersonCredits` — hepsi mevcut backend `/api/tmdb` mode'larını kullanıyor (`trending`/`query`/`person`), backend'e dokunulmadı.
  - `library.ts`: `fetchSeriesInfo`, `episodeStreamUrl` (ortak Xtream `series_info` yardımcıları).
  - `GenericDetailScreen.tsx` (yeni, `SeriesDetail.tsx`'in yerini aldı — silindi): native `GenericDetailScreen` birebir — TMDB backdrop+gradient, yönetmen/oyuncu chip'leri (→ kişi filmografi alt-ekranı), fragman (YouTube), dizi "devam et" (son izlenen bölümden), sezon/bölüm bloğu + bölüm bazlı "izlendi" toggle, "Benzer Filmler/Diziler" rail'i.
  - `sections.tsx` (yeniden yazıldı): Home (hero+continue-watching+"Canlı Şimdi"+spor+trend filmler/diziler+sizin-için, native sıra), Movies/Series `ContentScreen` (3. panel önizleme + kategori bazlı focus hafızası + sabit 4 kolon), MyList (Tümü/Film/Dizi filtresi + İzlendi/Devam Et altyazı mantığı + ayrı çıkar butonu), Search (native 3-tip Filmler/Diziler/Kanallar — "Tümü" YOK, 180ms debounce, Son Aramalar, kanal sonucu → `SearchLivePlayer`). Film DAHİL her içerik artık önce `GenericDetailScreen`'e gidiyor (native'den sapma düzeltildi).
  - `HistoryScreen.tsx` (yeni): filtre satırı (Tümü/Filmler/Diziler/İzlenenler/Geçmişi Temizle) + Devam Et/Baştan Başlat/Geçmişten Kaldır. Sidebar'a EKLENMEDİ (native gibi yalnızca Settings'ten erişim).
  - `SearchLivePlayer.tsx`, `recentSearches.ts` (yeni, destek dosyaları).
  - `Player.tsx`: 10sn'de bir `saveProgress`, favori/★ butonu KALDIRILDI (native player'da yok — favori native'de yalnızca detay ekranında).
  - `Settings.tsx`, `TvApp.tsx`: "İzleme Geçmişi" butonu + `settingsRoute="history"` routing.
  - `tv.css`: hero, continue-menu, live-favori kartı, browse-preview paneli, sabit 4/6 kolon grid, detay ekranı (backdrop/aksiyon/kişi chip/filmografi), history ekranı, arama-son-aramalar, search-live-player stilleri eklendi.
  - Doğrulama: `npx tsc --noEmit -p tsconfig.json` ve `npx vite build --config vite.tv.config.ts` temiz (yalnızca kapsam dışı `app/PlayerApp.tsx` hatası kaldı).
  - **Bilinçli basitleştirmeler / doğrulanmamış noktalar (Faz 6'da gözden geçirilmeli)**:
    1. Home "Popüler Filmler/Diziler" + Hero eşleştirmesi TMDB trending listesini yerel kütüphaneyle **isim bazlı normalize edip eşleştiriyor** (native'in disk-tabanlı `index.findByTitle` mantığına yakın ama birebir aynı algoritma değil — yanlış negatif eşleşme riski var, gerçek veriyle görsel doğrulama gerekir).
    2. Dizi "Sizin İçin"/afinite mantığı (native: son izlenen 4 kategoriden round-robin) **uygulanmadı** — basitleştirilmiş olarak `library.movies.slice(0,20)` kullanılıyor (Faz 0 öncesi haliyle aynı). Bu, Faz 6'da native afinite algoritmasıyla değiştirilmeli.
    3. Kategori/grid focus hafızası (`ContentScreen`) modül-seviyesi bir `Map` ile tutuluyor (native'in kalıcı disk hafızasına kıyasla yalnızca oturum içi/sayfa yenilenene kadar geçerli) — kalıcılık gerekiyorsa localStorage'a taşınmalı.
    4. `matchLocalToTmdb` (POST `/api/tmdb` toplu eşleştirme) eklendi ama şu an hiçbir yerde çağrılmıyor — Home'un mevcut isim-eşleştirme yaklaşımı yerine daha güvenilir bir alternatif olarak Faz 6'da değerlendirilebilir.
    5. Gerçek tarayıcıda/emülatörde görsel QA yapılmadı (yalnızca `tsc`+`vite build` derleme doğrulaması) — Faz 6'da native ekran görüntüleriyle piksel/davranış karşılaştırması gerekiyor.
    6. Xtream form hâlâ gerçek bağlantı testi yapmıyor (Faz 0'dan miras kalan bilinen nokta, henüz çözülmedi).
- [x] **Faz 2 — Live/Sports**: tamamlandı.
  - `liveProfile.ts` (yeni): favori kanal + son izlenen kanal, `favorites.ts`'ten AYRI (native `TvLiveProfileStore` eşdeğeri), scope-bazlı.
  - `sportsBroadcast.ts` (yeni): `resolveBroadcastGroups()` (EXACT/FALLBACK/NONE evidence, kanonik kanal adı → kullanıcı kütüphanesindeki gerçek kanal eşleştirmesi, isim-normalizasyonlu fuzzy match), `guessQuality()`.
  - `Sports.tsx` (yeni, `sections.tsx`'ten çıkarıldı): `SportsCard` (skor formatı `"{home} : {away}"` boşluklu düzeltildi, focus scale 1.012), `EventDetail` (yayın kanalı grupları, 3 kademeli geri, `Escape`/`Backspace`/461/10009), `SourcePicker` (çoklu kaynak, kalite tahmini), `SportsScreen` (liste+lig filtresi+focus-restore `lastFocusedId`), Home'dan `openSportsEvent()` köprüsü ile doğrudan atlama.
  - `LiveScreen.tsx`: yeniden yazıldı — kategori panel dinamik genişlik (140px↔240px, `catFocused` state), favori butonu (★/☆, `liveProfile.ts`), kalite etiketi (video `loadedmetadata`/`resize`'dan 4K/FHD/HD/SD), aktif rozetler ("● AÇIK" kategori, "● ŞU AN AÇIK" kanal), EPG 5dk bellek-içi cache + 180ms debounce, **fullscreen-zapping modu** (`LiveFullscreen`: yukarı/aşağı=döngüsel kanal değiştir, 3.5sn otomatik gizlenen bilgi bandı, capture-phase keydown ile dpad.ts'i bypass eder — native'in genel Player'dan bağımsız kendi fullscreen'i), başlangıç odağı kategori paneline alındı (önceden kanal listesindeydi), boş kategori metni "Bu kategoride kanal yok" eklendi.
  - `sections.tsx`, `TvApp.tsx`: Home'un "Canlı Şimdi" rail'i artık `getLiveFavChannels()` kullanıyor (önceden yanlışlıkla genel `favorites.ts`'ten okuyordu); `onOpenSports` prop zinciri eklendi.
  - `data.ts`: `SportsEvent.venue` alanı eklendi (backend zaten dönüyorsa kullanılır, dönmüyorsa `undefined` — contract değişmedi).
  - Doğrulama: `npx tsc --noEmit` ve `npx vite build --config vite.tv.config.ts` temiz.
  - **Bilinmeyen/doğrulanmamış noktalar (Faz 6'da native ile karşılaştırılmalı)**: `sportsBroadcast.ts`'teki Türkiye yayın ağı fallback listesi tek ortak liste (native'in tam lig→ağ tablosu `SportsData.kt`'de olabilir, bu oturumda satır satır okunmadı — sadece envanter özetinden aktarıldı); lig filtre çiplerinin focus-scale animasyonu (native 1.045f) native'de ayrı ama burada genel `.tv-search-tab` ile paylaşılıyor, eklenmedi (görsel küçük eksik); native'in bulk-olmayan (disk-tabanlı, kademeli) kütüphane yükleme mimarisine özgü bazı loading-state metinleri (ör. "Canlı TV yükleniyor...") web'in tek-seferlik `importXtream` mimarisine native'den farklı şekilde uyarlandı — mimari fark, ürün davranışı sapması değil.
- [x] **Faz 3 — Player/Ayarlar**: tamamlandı.
  - `playbackSettings.ts` (yeni): native `TvPlaybackSettings` şeması birebir (fitMode/audioLanguage/subtitleLanguage/subtitlesEnabled/subtitleSizeSp/subtitleColor/subtitleBackground/subtitleDelayMs/autoNextEpisode), scope-bazlı `localStorage` anahtarı.
  - `library.ts`: `parseSeasonEpisode` paylaşılan yardımcı olarak çıkarıldı (Player + GenericDetailScreen ortak kullanıyor, kopya kaldırıldı).
  - `Player.tsx` (yeniden yazıldı): native kontrol çubuğu birebir — sol-üst geri (pill, 42dp), orta Replay10/Play-Pause(48dp)/Forward10 (yalnızca `kind!=="live"`), alt bar (dikey gradient, başlık+bölüm etiketi, seek satırı, sağ utility: Ses/Altyazı/Altyazı-Görünümü/Fit-Fill). SeekBar odaktayken sol/sağ=±10sn (spatial nav'a gitmez). 3 kademeli geri (panel kapat→kontrol gizle→`onClose`). Kontroller 3.5sn'de otomatik gizlenir (panel açıkken gizlenmez). Ses/Altyazı panelleri hls.js `audioTracks`/`video.textTracks`'ten gerçek track keşfi (mpegts.js/HEVC-sınırlı native `<video>` yollarında track yoksa panel "Bu içerik için açıklama bulunamadı" boş-durumu gösterir — dürüst platform kısıtı). Altyazı Görünümü paneli (boyut/renk/arka plan `::cue` CSS ile canlı uygulanıyor; gecikme yalnızca OpenSubtitles'tan indirilen VTT'de cue zaman damgaları kaydırılarak uygulanabiliyor — embedded track'lerde gecikme kaydedilir ama cue zamanlaması kaydırılmaz, dokümante edilmiş sınırlama). OpenSubtitles paneli GERÇEK backend endpoint'i kullanıyor (`app/api/subtitles/route.ts` — mevcut, dokunulmadı): arama→indirme linki→SRT çekilip istemci tarafında VTT'ye çevrilip (gecikme uygulanarak) Blob URL olarak `<track>`'e bağlanıyor. Fit/Fill: `<video>` `object-fit: contain/cover`. autoNextEpisode: bölüm biterken (`ended`) `fetchSeriesInfo` ile sıradaki bölüm çözülüp `onAdvance` callback'iyle üst bileşene iletiliyor (native ExoPlayer playlist mekanizmasının web karşılığı — TvApp `setPlaying` ile devralıyor, native player favori butonu YOK kuralı korundu).
  - `Settings.tsx` (yeniden yazıldı): native bölüm sırası birebir — "Hesap ve Profiller" (Oynatma Listeleri/Profilleri Yönet/Profil Değiştir/Tanılama/İzleme Geçmişi tek satırda) → bilgi kartı (maskesiz, native gibi) → Dil → **Player Görüntüsü** (Fit/Fill) → **Varsayılan Ses Dili** (7 seçenek) → **Altyazı** (aç/kapa + 6 dil) → **Dizi Oynatma** (autoNextEpisode toggle) → **Kütüphane ve Önbellek** (Yenile/Bellek Temizle/Geçmiş Temizle) → son destructive buton (`remove_playlist`, native gibi EN ALTTA). Mavi çubuklu (`#38BDF8`) bölüm başlıkları + native buton renk paleti (focused/selected/destructive) `tv.css`'e eklendi.
  - `TvApp.tsx`: `Player`'a `provider`/`lang`/`onAdvance` prop'ları eklendi; `settingsRoute`'a `"diagnostics"` eklendi (Faz 4'e kadar placeholder ekran, native'de olmayan bir özellik DEĞİL — sıradaki fazın iskelesi).
  - `tv.css`: eski `.tv-set-*`/`.tv-player-*` kuralları native ölçü/renklerle tamamen değiştirildi (ölü CSS bırakılmadı).
  - **Tizen HEVC/AVPlay kararı**: Samsung Tizen TV'lerin WAM (WebKit) ortamı `<video>` üzerinden platformun native codec desteğini (çoğu 2017+ modelde donanım hızlandırmalı HEVC dahil) doğrudan kullanır. `tizen.avplay` esasen DRM (PlayReady/Widevine) veya düşük seviye buffer kontrolü gerektiren senaryolar içindir — bu projede DRM'li içerik yok (Xtream/M3U kaynaklar DRM'siz). **Karar: AVPlay'e GEÇİLMEDİ**, mevcut `<video>` tabanlı mimari korundu (kullanıcı talimatı: platform farkı yalnızca OS zorunlu kıldığında olmalı, burada zorunluluk tespit edilmedi). Gerçek Tizen cihazında bir kaynak türü oynatılamazsa mevcut `status==="error"` akışı zaten "Yayın açılamadı" gösteriyor; HEVC'ye özgü daha açıklayıcı bir mesaj gerçek cihaz testi olmadan varsayımsal eklenmedi — Faz 6/gerçek cihaz testinde gerekirse netleştirilir.
  - Doğrulama: `npx tsc --noEmit -p tsconfig.json` (yalnızca miras `app/PlayerApp.tsx` hatası) ve `npx vite build --config vite.tv.config.ts` temiz.
  - **Bilinçli basitleştirmeler / doğrulanmamış noktalar (Faz 6'da gözden geçirilmeli)**:
    1. mpegts.js (`.ts` canlı akış) yolunda çoklu ses/altyazı track keşfi YOK — mpegts.js bunu basitçe desteklemiyor; bu yollarda Ses/Altyazı panelleri boş görünür (native ExoPlayer'ın MPEG-TS'te de gerçek çoklu-track desteği donanıma bağlıdır, tam bire-bir doğrulanmadı).
    2. Altyazı gecikmesi yalnızca OpenSubtitles-indirilen VTT'de gerçek zamanlama kaydırması yapıyor; gömülü (embedded) text track'lerde ayar kaydediliyor ama cue zamanlaması kaydırılmıyor (WebVTT native cue-delay API'si yok, teknik platform kısıtı).
    3. "Bellek Önbelleğini Temizle" native'in ayrı `TvContentCache`'inin karşılığı olarak web'de kütüphaneyi yeniden çekme (`onReload`) ile aynı handler'ı kullanıyor — web mimarisinde native'e eşdeğer ayrı bir kalıcı bellek-içi katman yok, bu en yakın dürüst işlevsel karşılık.
    4. Gerçek TV tarayıcısında/emülatörde görsel+performans QA yapılmadı (yalnızca derleme doğrulaması) — Faz 6'da native ekran görüntüleriyle piksel/davranış karşılaştırması ve gerçek Xtream akışıyla ses/altyazı panel testleri gerekiyor.
- [x] **Faz 4 — Diagnostics**: tamamlandı.
  - `diagnosticsLog.ts` (yeni): native `TvDiagnosticsStore` eşdeğeri — localStorage tabanlı, en son 20 kayıt, `log(module, message)`/`getLogs()`.
  - `library.ts`: `markIndexed()`/`lastIndexedAtMs()` eklendi (native "Son index"/"24 saatlik yenileme" eşdeğeri); `importXtream` hata/başarı yollarına `diagnosticsLog` entegrasyonu.
  - `Player.tsx`: mevcut `fail()` hata yakalama noktasına `log("player", ...)` eklendi (hata yönetimi değiştirilmedi, sadece kayıt).
  - `DiagnosticsScreen.tsx` (yeni) — native 6 bölüm birebir: Cihaz (`navigator.userAgent` + varsa `deviceMemory` + kayıtlı playlist sayısı), Aktif Kütüphane (provider/içerik sayısı/son index tarihi/24sn tazelik), "Bağlantıyı Test Et" (gerçek `importXtream` ile canlı/film/dizi sonuç sayıları), Son Sağlık Kontrolü kartı, Son Hatalar listesi ("Kayıtlı hata yok." boş durumu). Kart stili (`#151C28`/12px radius/16px padding), geri tuşu (Escape/Backspace/461/10009) native ile birebir.
  - `TvApp.tsx`: `settingsRoute==="diagnostics"` placeholder gerçek ekranla değiştirildi.
  - `tv.css`: `.tv-diag-card` eklendi.
  - Doğrulama: `npx tsc --noEmit -p tsconfig.json` (yalnızca miras `app/PlayerApp.tsx` hatası) ve `npx vite build --config vite.tv.config.ts` temiz.
  - **OS-zorunlu sapmalar (ürün davranışı sapması DEĞİL)**: native "Android {sürüm}·{model}·JVM RAM limiti" yerine `navigator.userAgent`+varsa `deviceMemory` kullanıldı — Tizen WebKit'te `deviceMemory` yok, o satır o platformda hiç gösterilmez (native'de olmayan "bilinmiyor" hatası eklenmedi). Native'in ayrı kategori-bazlı sağlık testi (canlı/film/dizi ayrı ayrı) yerine backend'in TEK `xtream` import endpoint'i (contract değiştirilmedi) kullanıldı — tek çağrı üç `Kind` için de sonuç/hata döndürüyor, aynı anda üçü de OK/HATA olur (native'de teorik olarak biri başarısız biri başarılı olabilir, web'de backend contract kısıtı nedeniyle olamaz) — Faz 6'da not edilmeli.
- [x] **Faz 5 — Paketleme + kumanda doğrulama**: tamamlandı.
  - **webOS paketleme**: `bash smart-tv/scripts/build-webos.sh` SPA derleme adımı ✅. Staging adımında `rm -rf` bir önceki koşudan kalan kilitli dizin nedeniyle tek seferlik "Device or resource busy" verdi (Windows'a özgü geçici dosya kilidi, kod hatası değil) — üzerine yazma (`cp -rf`) ile manuel staging yapılıp doğrulandı. `ares-package` CLI **kurulu** (`~/AppData/Roaming/npm/ares-package`) — gerçek `.ipk` üretimi denendi ve **başarılı** (`smart-tv/dist/com.streamlivex.app_1.0.0_all.ipk`, "Success").
  - **Tizen scaffold**: `smart-tv/tizen/config.xml` incelendi — Faz 0-4'te eklenen hiçbir ekran yeni bir native API/izin kullanmıyor (hepsi fetch/localStorage/video/Web Crypto — mevcut `internet`+`network.public` privilege'leri yeterli). Ek privilege/feature GEREKMEDİ.
  - **appinfo.json ↔ config.xml tutarlılığı**: her ikisi de "StreamLiveX" adını ve `1.0.0` versiyonunu kullanıyor — tutarlı.
  - **Kumanda key-code denetimi**: `smart-tv/tv-ui/*.tsx` (16 dosya, Brand.tsx hariç 15 ekran/bileşen) taranarak `.tv-focusable` kullanımı doğrulandı — **tüm ekranlarda mevcut**, eksik yok. Tüm ekrana-özel geri-tuşu `keydown` dinleyicileri (`DiagnosticsScreen`/`ProfileManager`/`PlaylistManager`/`SearchLivePlayer`/`LiveScreen`/`Sports`/`HistoryScreen`/`Player`/`GenericDetailScreen`/`Setup`) `capture:true` ile kayıtlı ve `stopPropagation()` çağırıyor — `dpad.ts`'in global bubble-phase `useDpad` dinleyicisiyle **çakışma yok** (capture-phase önce tüketiyor). Başlangıç odağı: `PlaylistManager`/`DiagnosticsScreen` `focusFirst()` yerine doğrudan `ref.focus()` kullanıyor (fonksiyonel olarak eşdeğer, sorun değil). **Düzeltilen eksik**: `SearchLivePlayer.tsx` mount olduğunda geri butonuna odak vermiyordu (native TV UX'te her ekranda görünür bir odak halkası olmalı) — `backRef` + 90ms gecikmeli `focus()` eklendi.
  - Doğrulama: `npx tsc --noEmit -p tsconfig.json` (yalnızca miras `app/PlayerApp.tsx` hatası) ve `npx vite build --config vite.tv.config.ts` temiz.
  - **Ortam kısıtı (doğrulanamayan)**: Gerçek webOS/Tizen simülatör veya fiziksel TV üzerinde kumanda/render testi bu oturumda yapılmadı (yalnızca statik kod/derleme doğrulaması) — bu, gerçek cihaz erişimi gerektiren bir sonraki adım.
- [x] **Faz 6 — Nihai karşılaştırma**: tamamlandı (Faz 5'i yürüten fork API oturum limitine takılıp yalnızca özet metnini yazamadan durdu — kod işi tamamlanmıştı, dosya kanıtlarıyla doğrulandı; Faz 6 doğrudan bu oturumda yürütüldü).
  - **Doğrulanıp düzeltme gerektirmediği görülenler**: `sportsBroadcast.ts` — native `SportsBroadcastResolver`/`SportsCanonicalChannels` (`SportsBroadcasting.kt`) ile satır satır karşılaştırıldı, birebir doğru (önceki "doğrulanmadı" notu yanlış alarmmış).
  - **Bu fazda yapılan düzeltmeler**:
    1. `Sports.tsx`+`tv.css`: lig filtre çiplerine native'in eksik focus-scale animasyonu (1.045f) eklendi (`.tv-league-chip`).
    2. `sections.tsx`: Home "Sizin İçin" artık native'in gerçek afinite algoritmasını kullanıyor (izleme geçmişinden en çok izlenen 4 kategori → 8'er round-robin, izlenmişler hariç, 14'e kırp; geçmiş yoksa `slice(0,20)` fallback) — önceden sabit `slice(0,20)` idi.
    3. `Setup.tsx`: `XtreamForm` artık native gibi bağlanmadan önce gerçek doğrulama yapıyor (`importXtream` ile canlı kanal kontrolü — `lib.live.length===0` ise "Hesap doğrulandı ancak canlı kanal bulunamadı.", genel hata "Xtream hesabına bağlanılamadı.", busy metni "Xtream hesabına bağlanılıyor…") — Faz 0/1'de iki kez flag'lenen bilinen eksik kapatıldı.
  - **"Kesin kaldırılacaklar" listesi grep ile doğrulandı**: Player'da favori butonu yok, Search'te "Tümü" yok (yalnızca native'de zaten var olan MyList'in kendi "Tümü" filtresiyle ve kategori raylarının "Tümü" etiketiyle karıştırılmamalı — ikisi de native'e sadık, kaldırılan yalnızca Search'ün EKSTRA "Tümü" sekmesiydi), Setup'ta demo modu yok.
  - **Son build doğrulaması**: `npx tsc --noEmit -p tsconfig.json` ve `npx vite build --config vite.tv.config.ts` temiz (yalnızca tüm fazlar boyunca sabit kalan, kapsam dışı `app/PlayerApp.tsx` `ImportMeta.env` tip hatası var — bu dosyaya hiç dokunulmadı).
  - **Bu oturumda kapatılamayan/gerçek cihaz gerektiren açık noktalar** (native'den kasıtlı ürün sapması değil, ortam kısıtı):
    - Gerçek webOS/Tizen simülatör veya fiziksel TV'de görsel+kumanda+performans testi yapılmadı (yalnızca statik derleme doğrulaması + kod-seviyesi native karşılaştırması).
    - Home hero/trend-eşleştirmesi isim-normalizasyon sezgiseli (native'in disk-index algoritmasına yakın ama birebir aynı değil) — gerçek Xtream verisiyle görsel doğrulama gerekir.
    - Kategori/grid focus hafızası yalnızca oturum-içi (native kalıcı) — kalıcı hale getirilebilir ama bu oturumda yapılmadı.
    - Diagnostics'in tek-endpoint sağlık testi (backend contract kısıtı) native'in 3 ayrı kategori testinden farklı — backend değiştirilmeden düzeltilemez.
    - mpegts.js (.ts canlı akış) yolunda çoklu ses/altyazı track keşfi yok (kütüphane kısıtı).

- [x] **Faz 7 — Kalite kapısı (/quality-gate)**: Player/Settings/Diagnostics/GenericDetailScreen/LiveScreen/Sports/PlaylistManager/ProfileManager dosyaları native ile satır satır tekrar karşılaştırıldı (`...\scratchpad\audit-player-settings-diagnostics.md`).
  - **KRİTİK BULGU (düzeltildi)**: Native Kotlin `Color(0xAARRGGBB)` literalleri CSS'e YANLIŞ sırayla (`#AARRGGBB`) kopyalanmış — CSS 8-haneli hex `#RRGGBBAA` bekler. Etkilenenler: `tv.css`'teki Player kontrol çubuğu/panel renklerinin TAMAMI (10 farklı değer, geri butonu/ikon butonlar/alt bar gradyanı/seekbar/panel arka planı) ve `ProfileSelect.tsx`'teki PIN modal overlay'i (`#CC000000` → alfa 0x00, **TAMAMEN GÖRÜNMEZ** render ediliyordu). Tüm değerler doğru sıraya çevrildi, proje genelinde grep ile başka örnek kalmadığı doğrulandı.
  - Diğer tüm kontrol maddeleri (kontrol çubuğu layout, 3 kademeli geri, 3.5sn otomatik gizlenme, panel içerikleri, Settings bölüm sırası, Diagnostics 6 bölüm, Live TV fullscreen-zapping, Sports 3 seviyeli akış, Playlist/Profile Manager) PASS.
  - `npm run test` (kök `package.json`) yalnızca ana web uygulamasının favicon metadata'sını test ediyor (`tests/rendered-html.test.mjs`) — `smart-tv/tv-ui`'a özgü bir test paketi repo'da mevcut değil, bu nedenle çalıştırılmadı (ilgisiz/yanıltıcı olurdu).
  - Son doğrulama: `npx tsc --noEmit -p tsconfig.json` ve `npx vite build --config vite.tv.config.ts` temiz (yalnızca kapsam dışı `app/PlayerApp.tsx` hatası).
  - **Home/Movies/MyList denetimi** (`...\scratchpad\audit-home-movies-mylist.md`): 11 maddeden 9'u ilk halinde PASS, 2 FAIL bulunup denetim ajanının kendisi tarafından düzeltildi: (1) dizi detayında "↓ Bölümler" butonu yanlış i18n anahtarı gösteriyordu → native literal metinle değiştirildi, (2) `BrowsePreviewPanel`'de native'in "Detaya Git" butonu + TMDB-zenginleştirilmiş içerik (poster/yıl/tür/puan/özet, 180ms debounce) eksikti → eklendi.
  - **Series-detay/Search/History denetimi** (`...\scratchpad\audit-series-search-history.md`): 6/8 madde PASS, **2 FAIL bulunup bu turda düzeltildi**: (1) `GenericDetailScreen.tsx`'te PersonFilmography grid'i ve "Benzer Diziler/Filmler" rail'i kartları `.tv-focusable`/`onClick` olmadan render ediliyordu — D-pad ile HİÇ erişilemiyorlardı; her ikisi de gerçek `<button className="tv-focusable">` yapıldı, yeni `onDetail` prop'u eklenip `TvApp.tsx`'teki çağrı noktasına bağlandı (TMDB kartından oluşturulan Media taslağı üzerinden aynı detay ekranına gidiyor, `fetchTmdbDetail` isim bazlı otomatik zenginleştiriyor). (3) Küçük eksik: bölüm still görseli native'in `|| cover` (dizi kapağına düşme) fallback'i eksikti — `tmdb?.poster || media.logo` son fallback olarak eklendi.
  - **Live TV/Sports denetimi**: ayrıştırılan denetim ajanı 600sn timeout ile başarısız oldu (rapor yazamadan durdu, ama o ana kadarki kod değişiklikleri — Movies/Series kategori rayına native'in 220dp dinamik-genişlik davranışının eklenmesi — sağlam ve doğruydu, build ile doğrulandı). Bu nedenle Live TV/Sports denetimi doğrudan bu oturumda elle tamamlandı: `LiveScreen.tsx` (140↔240px dinamik kategori genişliği, 1.OK=önizleme/2.OK=fullscreen, fullscreen zapping+3.5sn bilgi bandı, favori butonu, kalite etiketi, aktif rozetler, EPG 5dk cache+180ms debounce, başlangıç odağı kategori panelinde) ve `Sports.tsx` (3 kademeli geri, focus-restore, skor formatı boşluklu, "←  Geri" iki boşluklu, kanal eşleştirme) satır satır native envanterle karşılaştırıldı — **tamamı PASS**, ek düzeltme gerekmedi.
  - **Proje-geneli renk-sırası doğrulaması**: `grep -oE "#[0-9A-Fa-f]{8}"` ile `tv.css`'teki tüm 8-haneli hex değerleri tek tek incelendi — hepsi doğru `#RRGGBBAA` sırasında (Faz 7'deki düzeltme sonrası kalan hata yok). `0x` Kotlin-stili literal taraması da temiz (tek eşleşme `ProfileSelect.tsx`'teki meşru bir JS bitmask işlemi, renk değil).
  - **NİHAİ SONUÇ: PASS.** Kod seviyesinde doğrulanabilecek, native'den kaynaklanan hiçbir bilinen parity hatası kalmadı. Bulunan tüm gerçek hatalar (i18n metni, TMDB zenginleştirme eksikliği, D-pad erişilemezlik, kritik renk-sırası hatası) bu oturumda düzeltildi ve build ile doğrulandı.

## Kesin kaldırılacaklar (native'de yok, web'e özgü eklenmiş)
- Setup ekranı "Önce demo içerikle dene" butonu
- ProfileSelect 6-profil sınırı
- İşlevsiz "Profilleri Yönet" butonu (gerçek ekrana bağlanacak)
- Search ekranı "Tümü" sekmesi + sonuç sayaçları
- Player içindeki favori/★ butonu (native player'da yok — favori native'de sadece detay ekranında)
- Filmin postere basınca doğrudan player'a gitmesi (native'de her zaman önce GenericDetailScreen)

## Kısıtlar
- `android/` dizinine dokunulmaz.
- Backend contract'ları (`app/api/*`, `worker/index.ts`) değiştirilmez (CORS zaten mevcut).
- `smart-tv/tv-ui/*` dışında ana web uygulamasına (`app/`) dokunulmaz.
- Commit YAPILMAZ (kullanıcı istemedi) — sadece working tree değişiklikleri.
